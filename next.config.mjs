/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    /*
      If you are deploying a static site, enable output: 'export'
      output: 'export',
    */
};

export default nextConfig;
